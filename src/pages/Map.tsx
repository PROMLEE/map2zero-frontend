import styled from 'styled-components';

export default function Map() {
  return <HomeBackground>지도</HomeBackground>;
}
const HomeBackground = styled.div`
  position: relative;
  height: 95vh;
  width: 100vw;
  overflow: hidden;
`;
